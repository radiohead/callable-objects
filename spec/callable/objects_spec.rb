require "spec_helper"

RSpec.describe Callable::Objects do
  it "has a version number" do
    expect(Callable::Objects::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
